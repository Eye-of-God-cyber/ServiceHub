/**
 * services/index.ts
 *
 * Barrel export for API service modules.
 *
 * Services to be added in future milestones:
 *   • authService    — register, login, getMe
 *   • userService    — getProfile, updateProfile, addresses CRUD
 *   • providerService
 *   • catalogService — categories, services
 *   • bookingService
 *   • walletService
 *   • reviewService
 *   • notificationService
 *   • disputeService
 *   • adminService
 *
 * Each service file imports the shared `api` Axios instance from @/lib/api
 * and the relevant API_ROUTES constants from @/constants.
 */
