/**
 * utils/index.ts
 *
 * Barrel export for utility/helper functions.
 *
 * Utilities to be added in future milestones:
 *   • formatCurrency(amount, currency)  — Decimal → "₹ 1,250.00"
 *   • formatDate(iso, options)          — ISO string → localised date string
 *   • truncate(str, maxLen)             — shorten long strings with "…"
 *   • getInitials(firstName, lastName) — "John Doe" → "JD" (for avatars)
 *   • buildQueryString(params)          — object → "?page=1&limit=20"
 */
