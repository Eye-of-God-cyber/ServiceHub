/**
 * features/index.ts
 *
 * Barrel export for domain feature modules.
 *
 * Each feature folder will contain:
 *   components/   — feature-specific UI components
 *   hooks/        — feature-specific hooks (useBookings, useWallet, …)
 *   types.ts      — business model types for this feature
 *   schema.ts     — Zod validation schemas
 *
 * Features to be added in future milestones:
 *   • auth/
 *   • catalog/
 *   • bookings/
 *   • wallet/
 *   • reviews/
 *   • notifications/
 *   • disputes/
 *   • providers/
 *   • admin/
 */
